import styled from 'styled-components';

export const Content = styled.header`
  display: flex;
  flex-direction: column;
  align-items: center;

  padding: 0;

  img {
    width: min(24rem, 100%);
  }
`;
